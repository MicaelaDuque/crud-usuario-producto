<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie add</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <figure>
        <img src="/img/videoclub.jpg" alt="Logo Video Club">
      </figure>
    
      <body>
        <main>
        
          <!-- form con usuario y password -->
          <form id="form" action="/login" method="post" class="register-form"  >
            <h3 class="register-tittle">Ingresar con Contraseña</h3>
            <label for="email" class="register-label"> Email</label>
            <input
              type="email"
              id="email"
              name="email"
              placeholder="Email"
              class="register-input"
            />
            <% if (locals.error && error.email) { %>
             <p style="color: red;"><%= error.email.msg %></p>
            <% } %>
      
      
            <label for="password" class="register-label">Contraseña:</label>
            <input
              type="password"
              id="password"
              name="password"
              placeholder="contraseña"
              class="register-input"
            />
            <% if (locals.error && error.password) { %>
              <p style="color: red;"><%= error.password.msg %></p>
             <% } %>
    
             
            
            <button type="submit" class="submit">Iniciar sesion</button>
            
          </form>
        </main>
      
      </body>

</html>